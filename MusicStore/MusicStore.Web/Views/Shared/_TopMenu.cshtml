<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="@Url.Action("Index", "Home")">Music Store</a>
        </div>

        @{
            var controllerName = ViewContext.RouteData.Values["controller"].ToString();

            <div class="collapse navbar-collapse" id="navbar">
                <ul class="nav navbar-nav">
                    <li class="@(controllerName == "Home" ? "active" : "")">
                        <a href="@Url.Action("Index", "Home")">Home</a>
                    </li>
                    <li class="@(controllerName == "Admin" ? "active" : "")">
                        <a href="@Url.Action("Index", "Admin")">Admin</a>
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li>
                        <a href="@Url.Action("RefreshSchema", "Admin")">Refresh Schema</a>
                    </li>
                    <li>
                        <a href="@Url.Action("PopulateDb", "Admin")">Populate DB</a>
                    </li>
                </ul>
            </div>
        }
    </div>
</nav>